<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection</title>
    <link rel="stylesheet" href="/css/main.css"/>
</head>
<body>
  <%- include('partials/navbar' , {page:'/lego/sets'}) %>
  <div class="container mx-auto p-4">
  <div class="hero bg-base-200 rounded-lg p-6">
    <div class="hero-content text-center">
      <div class="max-w-md">
        <h1 class="text-5xl font-bold">View Collection</h1>
        <p class="py-6">Check out the collection. Select the theme you want to filter by:
          <a href="/lego/sets?theme=Technic" class="btn btn-primary btn-xs">Technic</a>
          <a href="/lego/sets?theme=Playhouse" class="btn btn-primary btn-xs">Playhouse</a>
          <a href="/lego/sets?theme=Set" class="btn btn-primary btn-xs">Set</a>
        </p>
      </div>
    </div>
  </div>
  </div>
  <br>
  <div class="container mx-auto p-4">
  <div class="overflow-x-auto">
    <table class="table table-zebra">
      <tbody>
        <% sets.forEach(set => { %>
          <tr class="border-b">
            <td class="p-4">
              <img src="<%= set.img_url %>" alt="<%= set.name %>" class="w-20 h-20 object-cover rounded">
            </td>
            <td class="p-4">
              <div class="flex items-center">
                <h2 class="text-lg font-bold"><%= set.name %></h2>
                <span class="mx-2">|</span> <!-- Separator -->
                <a href="/lego/sets?theme=<%= set.theme %>" class="btn btn-sm btn-<%= set.theme.toLowerCase() %>">
                    <%= set.theme %>
                </a>
            </div>            
              <p class="text-sm text-black">Year: <%= set.year %></p>
              <p class="text-sm text-black">Parts: <%= set.num_parts %></p>
            </td>
            <td class="p-4">
              <a href="/lego/sets/<%= set.set_num %>" class="btn btn-sm btn-primary">
                Details
              </a>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>
</body>
</html>